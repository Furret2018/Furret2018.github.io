Course: Linux Foundation Certified System Administrator (LFCS)
==============================================================


## Lecture: Boot, Reboot, and Shut Down a System Safely

which shutdown
ls -lhF /sbin | grep shutdown
ls -lhF /sbin | grep 'shutdown\|poweroff\|halt\|reboot'
sudo shudown -r now
uptime


## Lecture: Boot or Change System into Different Operating Modes
